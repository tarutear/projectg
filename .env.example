# OpenCV.js source URL
# Development default: /opencv/opencv.js (local file in public/opencv/)
# Production: set to CDN URL so the file does not need to be committed to git
NEXT_PUBLIC_OPENCV_URL=https://docs.opencv.org/4.x/opencv.js
